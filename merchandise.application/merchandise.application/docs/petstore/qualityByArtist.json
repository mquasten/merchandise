{
   "language": "javascript",
   "views": {
       "qualityByArtist": {
           "map": "function(doc) {
               if(doc.artist && doc.quality) { 
                   emit(doc.artist.toLowerCase(), doc.quality.toLowerCase());
               }
           }"
       }
   }
}